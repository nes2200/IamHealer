using TMPro;
using UnityEngine;

public class UI_SaveSlot : MonoBehaviour
{
    [SerializeField] 

    bool _isSave;
    public bool IsSave => _isSave;

    public void ChangeText()
    {
        if( _isSave)
        {
        }
    }
}
